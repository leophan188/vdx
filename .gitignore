.claude/settings.local.json

# Bí mật go-live — KHÔNG commit
.env

# Build artifacts & dữ liệu cục bộ — KHÔNG commit (tự sinh khi build/run)
**/target/
**/node_modules/
**/dist/
backend/data/
vts_db_new.sql

# Rác hệ điều hành
.DS_Store
**/.DS_Store

# Bộ skill của công cụ AI (cài trên máy lập trình viên, không thuộc mã nguồn sản phẩm)
.agents/
