---
scope: Nền tảng BPM cấu hình động (process+form), on-prem 100-500 user
purpose: build-substrate
altitude: feature
updated: 2026-06-24T14:22
---

- (version) Java 21 (LTS) — nền tảng runtime.
- (version) Spring Boot 3.5.x — CHỌN bản 3.5 (cuối dòng 3.x, ổn định, tương thích Flowable 7) thay vì Spring Boot 4.1 mới (Flowable 7 build cho Spring Boot 3.x, tương thích SB4 chưa kiểm chứng). Ưu tiên ổn định cho on-prem.
- (version) Flowable 7.x (open-source, AGPL/Apache, nhúng JAR vào Spring Boot 3) — engine BPMN, nguồn sự thật cho trạng thái thực thi quy trình.
- (version) Angular 21 (LTS, hỗ trợ tới 5/2027) — CHỌN bản LTS thay vì Angular 22 mới, ưu tiên vòng đời bảo trì dài cho on-prem.
- (version) MariaDB 11.8 (LTS Community) — JSON path + JSON Schema validation sẵn có.
- (version) OnlyOffice Docs Community 9.4+ (AGPL v3) — QUAN TRỌNG: bản 9.4 (5/2026) đã GỠ giới hạn 20 kết nối đồng thời + kiến trúc single-process nhẹ hơn (bỏ RabbitMQ/DB phụ thuộc). Giải quyết rủi ro lớn nhất của PRD (NFR-02).
- (event) PHẢN HỒI NGƯỢC PRD: rủi ro 20-kết-nối OnlyOffice (NFR-02 / DEFER) đã hết hiệu lực — bản 9.4+ bỏ giới hạn. Cần cập nhật PRD.
- (decision) AD: Modular monolith on-prem — 1 deployable Spring Boot (BE) + Angular SPA (FE) + MariaDB + Flowable nhúng; OnlyOffice là service riêng. Binds=all. Prevents=phân rã microservices sớm gây phức tạp vận hành cho 100-500 user.
- (decision) AD: Flowable là NGUỒN SỰ THẬT DUY NHẤT cho trạng thái thực thi quy trình (process instance, token, định tuyến task). Cấm tự xây state machine song song. Binds=A,D,F. Prevents=hai engine trạng thái quy trình.
- (decision) AD: Đồng-tồn-tại phiên bản — định nghĩa process & form là BẤT BIẾN, có version; mỗi task instance SNAPSHOT id phiên bản định nghĩa lúc khởi tạo (Flowable deploymentId cho process; formDefinitionVersionId cho form). Runtime luôn resolve instance đang chạy theo phiên bản đã snapshot. Binds=FR-A06,FR-B09. Prevents=sửa định nghĩa làm vỡ instance đang chạy.
- (decision) AD: Lưu form động HYBRID — payload form lưu JSON column trên instance; trường gắn cờ 'reportable' trong định nghĩa form được CHIẾU (khi ghi) sang bảng quan hệ có chỉ mục. Báo cáo (nhóm G) CHỈ đọc bảng chiếu, không truy vấn JSON đường nóng. Binds=NFR-10,NFR-12,FR-G. Prevents=báo cáo query JSON (chậm) + lưu trữ phân tán mỗi form mỗi kiểu.
- (decision) AD: 'Quá hạn' là CỜ boolean trực giao (tính toán), KHÔNG phải giá trị enum trạng thái. Enum trạng thái={Chờ phê duyệt,Đang xử lý,Đã hoàn thành,Hủy}. Binds=FR-D03,D07. Prevents=quá hạn đè mất trạng thái nghiệp vụ.
- (decision) AD: Audit append-only — 1 bảng audit ghi-một-lần, phân vùng theo thời gian; mọi thay đổi trạng thái/dữ liệu phát audit event qua MỘT audit-writer port. Binds=FR-I01,NFR-13. Prevents=ghi audit rải rác, audit khả biến.
- (decision) AD: Org model — cây độ sâu tùy ý lưu bằng closure-table (hoặc adjacency+path); VỊ TRÍ là đơn vị phân công (late-binding resolve về người đang giữ), mỗi vị trí 1 người tại 1 thời điểm; task SNAPSHOT người được resolve lúc giao (FR-C05). Binds=C. Prevents=biểu diễn org phân kỳ, mơ hồ phân công.
- (decision) AD: OnlyOffice tích hợp qua cổng ports/adapter — Document Server là service riêng; APP sở hữu lưu trữ tài liệu + version, giao tiếp qua hợp đồng JWT callback. Vòng đời tài liệu KHÔNG phụ thuộc editor. Binds=FR-E01,E08. Prevents=ràng buộc vòng đời tài liệu vào editor.
- (decision) AD: Xác thực — tài khoản nội bộ (GĐ1), không SSO/AD; phiên/token; RBAC resolve qua vị trí->vai trò. Binds=FR-C06,C07.
- (decision) AD: Thông báo — 1 outbound port, kênh cắm-được (in-app, email); event publish 1 lần rồi fan-out. Binds=FR-H. Prevents=logic thông báo rải rác.
- (decision) AD: Ranh giới module + chiều phụ thuộc — feature module (process-config, form, org, task-exec, drafting, collaboration, reporting, notification, audit) phụ thuộc VÀO TRONG core dùng chung (domain+Flowable+persistence); cấm feature->feature trực tiếp, chỉ qua domain event/core. Prevents=coupling chéo feature không tương thích.
- (decision) AD: Backup nhất quán đa kho — binary tài liệu lưu ở filesystem/object-store có tham chiếu giao dịch trong DB; snapshot backup phối hợp cùng điểm thời gian DB+file. Binds=NFR-11.
- (direction) INPUT C1 (Architect): nền tảng generic (Flowable + form metadata) là MỘT, không đổi theo lựa chọn scope. Khuyến nghị: XÂY nền generic từ đầu (Flowable cho gần như miễn phí) NHƯNG GIAO HÀNG theo lát cắt dọc — chạy quy trình mẫu #1 end-to-end trước, rồi mở rộng độ phủ loại-trường/loại-luồng + affordance designer. => C1 là quyết định TRÌNH TỰ GIAO HÀNG, không phải nền tảng. Hòa giải: nền generic ngày 1, bề mặt tăng dần.
- (event) Hoàn tất elicit Fast-path; sẵn sàng distill spine.
- (decision) AD-13: Trạng thái nghiệp vụ là projection thuần — 1 StatusProjectionWriter từ Flowable listener; không service set trực tiếp. (đóng lỗ critical #1)
- (decision) AD-14: App TASK_ASSIGNMENT (snapshot) = nguồn sự thật phân công; Flowable assignee mirror qua AssignmentPort; reassign/delegate ghi app+Flowable 1 giao dịch; vị trí trống set candidate-group=đơn vị + cờ UNASSIGNED. (đóng critical #2 + #8)
- (decision) AD-15: Cancel cascade chỉ qua Flowable delete->execution-listener; app không xoá nhánh ngoài engine. (đóng #5)
- (change) Siết AD-3 (snapshot form tại mọi node kể cả collab sub-task), AD-4 (projection đồng bộ cùng giao dịch + reconcile job), AD-5 (overdue materialize cột indexed), AD-6 (Flowable history KHÔNG phải audit). Theo reviewer đối kháng 8 lỗ.
- (event) spine finalized
